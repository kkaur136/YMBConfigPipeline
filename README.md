# YMBConfigPipeline
A CI testing pipeline for the YMB banking application.

This repository demonstrates a GitHub Actions CI pipeline for the Yarrow-Mullein Bank banking application. Every push and pull request automatically runs the Python tests and records the result. This gives Ben Secure a Git-based audit trail showing what changed, who submitted the change, when the tests ran, and whether they passed or failed before the code reached production.
